# Django и DRF
Django==4.2.11
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.0  # для JWT токенов

# База данных
psycopg2-binary==2.9.9  # для PostgreSQL
# или для SQLite (проще для хакатона):
# ничего не нужно

# CORS (чтобы фронтенд мог обращаться к бэкенду)
django-cors-headers==4.2.0

# Утилиты для разработки
django-extensions==3.2.3  # дополнительные команды manage.py
python-dotenv==1.0.0  # для работы с .env файлами

# Для импорта/экспорта (опционально)
django-import-export==3.3.0

# Для работы с UUID
shortuuid==1.0.11

# Для валидации данных
drf-yasg==1.21.7  # документация Swagger

# Сервер разработки
gunicorn==21.2.0  # для продакшена